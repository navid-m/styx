[project]
name = "hydra"
version = "0.1.0"
description = "Wine/Proton prefix game launcher"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.6.0",
    "requests>=2.28.0",
]
